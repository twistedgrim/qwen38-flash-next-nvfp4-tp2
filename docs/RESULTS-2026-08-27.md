# Validated recipe result — 2026-08-27

This receipt records a clean start from the repository scripts on the GX10 pair.
It is not a claim that unrelated hardware will produce identical numbers.

## Artifact and infrastructure gates

- Pinned SGLang image present on both nodes.
- Checkpoint revision API resolved to
  `7b719225242aacd3dbd3f9407468c2ee9a9d2594`.
- 419 pinned repository files checked by path and size on both nodes.
- 206 indexed weight shards present; indexed weight bytes: 135,195,303,851.
- Head/worker fabric addresses and MTU 9000 passed.
- RoCE HCA `rocep1s0f0` and GID index 3 passed on both nodes.
- QSA source hash and generated overlay passed on both nodes.

## Clean recipe start

`./qwen38 serve` synchronized the recipe, stopped its labeled containers,
started worker then head, and reached `/health` without manual intervention.
Both containers subsequently reported zero restarts and no OOM.

The container environments explicitly contained:

```text
NCCL_NET=IB
NCCL_IB_DISABLE=0
NCCL_IB_HCA=rocep1s0f0
NCCL_IB_GID_INDEX=3
NCCL_IB_ROCE_VERSION_NUM=2
NCCL_SOCKET_IFNAME=enp1s0f0np0
GLOO_SOCKET_IFNAME=enp1s0f0np0
SGLANG_HOST_IP=10.76.0.1 or 10.76.0.2
```

During the final smoke, long-context, and benchmark gates, each node's RoCE
port transmitted and received approximately 108.24 GiB according to the IB
port counters. This verifies active fabric traffic rather than configuration
alone.

Runtime allocation per rank:

```text
ModelOpt NVFP4 weights
BF16 Mamba cache: 447 states, 0.46 GiB convolution + 11.81 GiB SSM
BF16 KV cache: 1,121,792 tokens, 6.42 GiB K + 6.42 GiB V
MTP: disabled
context: 262,144
max running requests: 4
```

## Correctness gates

`./qwen38 smoke` passed:

- exact plain-text response;
- separated reasoning content;
- structured tool call with valid JSON arguments;
- tool-result replay;
- four concurrent tool-carrying requests;
- observed maximum scheduled requests: 3;
- observed maximum Mamba usage: 0.027;
- no token-ID-0 bang runs or NaN/Inf markers.

The strict three-needle long-context gate passed:

| Target | Actual prompt tokens | Elapsed |
| ---: | ---: | ---: |
| 10,000 | 10,062 | 4.84 s |
| 60,000 | 60,787 | 21.76 s |
| 120,000 | 120,103 | 43.23 s |

Each prompt contained distinct early, middle, and late markers. All three had
to be returned in order, and server-reported prompt usage had to remain within
10% of the target.

## Warm-cache throughput gate

Two measured 256-token runs followed an explicit warmup at each concurrency:

| Concurrency | Aggregate output tok/s | Per-stream decode tok/s | TTFT |
| ---: | ---: | ---: | ---: |
| C1 | 26.10 | 26.38 | 0.144 s |
| C2 | 54.20 | 27.41 | 0.144 s |
| C4 | 94.87 | 24.01 | 0.174 s |

All configured minimum rates passed. This benchmark does not flush the radix
cache and must be compared only with the same recipe command and prompt suite.

## Final state

- `/health`, `/v1/models`, and `/metrics`: healthy.
- Model ID: `qwen3.8-flash-next-nvfp4`.
- Management-network request from an external client: passed with separated
  reasoning and final `PI_ENDPOINT_OK` content.
- Head and worker: running, zero restarts, no OOM, no matching CUDA/NCCL/NaN/
  token-ID-0 errors in final logs.
