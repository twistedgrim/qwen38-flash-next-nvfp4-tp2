# llama-swap and LiteLLM validation — 2026-08-27

Validated path:

```text
Pi → LiteLLM gx10-qwen3.8-flash-next
   → llama-swap qwen3.8-flash-next-nvfp4
   → SGLang TP2
```

## llama-swap

- The model is a member of the existing exclusive `gx10-tp2` group.
- Lifecycle commands point to this repository's `integrations/llama-swap/run.sh`
  and `qwen38 stop` interfaces.
- A request from an unloaded/manual state completed a full cold transition in
  513.48 seconds and returned `LLAMA_SWAP_QWEN38_OK`.
- llama-swap allocated backend port 5803 for the validated run.
- Head and worker received the same dynamic port.
- Both ranks retained 262,144 context and MTP disabled.
- Qwen uses a four-hour idle TTL; requests for another exclusive-group model
  still trigger an immediate swap.
- The global llama-swap health-check window and Qwen LiteLLM backend timeout are
  900 seconds to cover measured nine-minute cold starts.

The backend port is intentionally not stable. Clients use llama-swap `:8888`;
`:30000` is available only when the recipe is launched manually outside
llama-swap.

## LiteLLM

- Stable client alias: `gx10-qwen3.8-flash-next`.
- Backend model: `openai/qwen3.8-flash-next-nvfp4`.
- Context metadata: 262,144 input+output tokens.
- Advertised maximum output: 65,536 tokens.
- API authentication references the existing injected
  `GX10_VLLM_API_KEY`; no key is stored in this repository.
- Plain completion passed with separated reasoning.
- Forced structured tool call passed with valid JSON arguments.

The deployment has database-backed dynamic model creation disabled, so the
alias was added to the authoritative `litellm-config` ConfigMap and the LiteLLM
deployment was rolled out. A protected pre-change ConfigMap backup remains on
the LiteLLM host.

## Pi

The LiteLLM catalog refresh preserved model-info context lengths and added the
new alias at 262,144 tokens. A non-interactive Pi request through
`litellm/gx10-qwen3.8-flash-next` returned `PI_LITELLM_QWEN38_OK`.
